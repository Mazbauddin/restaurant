import Image from "next/image";
import React from "react";

const Menu = () => {
  return (
    <div>
      <Image src="/open.png" alt="Open" width={20} height={20} />
    </div>
  );
};

export default Menu;
